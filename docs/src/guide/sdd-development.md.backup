# SDD开发

## 概述

SDD（Specification-Driven Development，规范驱动开发）是一种以高质量、结构化的需求规范文档为核心驱动力的软件开发方法论。它将传统"先写代码再补文档"的流程反转，强调 "先定义清楚做什么，再决定怎么做，最后自动生成实现"。

## 核心理念
- 规范即代码"（Specification as Code）
- 将需求、设计、验收标准等以机器可读、AI可理解的格式编写
- 规范文档不仅是沟通工具，更是生成代码的输入源
- 开发者角色从"编码者"转变为"规范定义者 + 质量把控者"
- **设计先行**: 以设计稿为起点，确保UI/UX的一致性
- **组件化思维**: 基于微组件库构建，提高代码复用率
- **规范统一**: 统一的设计规范和开发规范，降低维护成本
- **快速迭代**: 缩短从设计到上线的时间周期

## SDD 与传统开发模式对比

SDD（规范驱动开发）与传统开发模式在多个维度上存在显著差异：

| 维度 | 传统开发 | SDD（规范驱动开发） |
|------|---------|-------------------|
| 起点 | 模糊需求 → 直接编码 | 清晰规范 → 自动生成代码 |
| 文档地位 | 事后补充，常滞后或缺失 | 核心资产，驱动整个流程 |
| 开发者重心 | 写代码、调 Bug | 定义需求、审查规范、验证结果 |
| AI 角色 | 辅助补全 | 主力执行者（基于规范生成实现） |
| 迭代方式 | 修改代码 → 测试 → 修复 | 修改规范 → 重新生成 → 验证 |

### 对比分析

#### 1. 开发起点
- **传统开发**：往往基于模糊的需求直接开始编码，导致频繁返工
- **SDD 开发**：先建立清晰的规范文档，再基于规范生成代码，减少返工

#### 2. 文档地位
- **传统开发**：文档往往是事后补充，容易滞后甚至缺失
- **SDD 开发**：文档是核心资产，整个开发流程由文档驱动

#### 3. 开发者角色转变
- **传统开发**：开发者主要精力在写代码和调试 Bug
- **SDD 开发**：开发者专注于定义需求、审查规范、验证 AI 生成的结果

#### 4. AI 的作用
- **传统开发**：AI 仅作为代码补全辅助工具
- **SDD 开发**：AI 是主力执行者，基于规范自动生成高质量代码

#### 5. 迭代效率
- **传统开发**：修改代码、测试、修复，周期长
- **SDD 开发**：修改规范文档，重新生成代码，快速验证

## 组件库

MicroAdmin 提供完整的组件库支持：

### 基础组件
- 按钮、输入框、选择器等常用表单组件
- 表格、树形控件、标签页等数据展示组件
- 弹窗、抽屉、消息提示等交互组件

### 业务组件
- 表单生成器
- 搜索组件
- 操作按钮组
- 数据字典选择器

### 布局组件
- 页面容器
- 卡片容器
- 分割面板

## SDD 开发流程

SDD 开发采用标准化的 8 步流程，确保需求清晰、设计合理、代码高质量。

| 步骤 | 命令 | 输出 | 说明 |
|------|------|------|------|
| 0. 项目章程 | `/speckit-constitution` | `constitution.md` | 定义技术栈约束、安全策略、编码规范、架构原则，相当于项目的"宪法" |
| 1. 功能规范 | `/speckit-specify` | `spec.md` | 用自然语言描述用户故事、业务规则、验收标准，回答"要做什么？"明确功能目标、验收标准 |
| 2. 需求澄清 | `/speckit-clarify` | 更新 `spec.md` | 针对不清晰的地方提问，AI识别模糊点，补充边界条件，消除歧义，提升规范的精确性和完整性 |
| 3. 技术方案 | `/speckit-plan` | `plan.md` | 接口设计、数据模型，制定架构设计、API 接口、数据库模型、技术选型，回答"怎么做？" |
| 4. 完整性检查 | `/speckit-checklist` | `checklist.md` | 检查遗漏场景 |
| 5. 任务拆解 | `/speckit-tasks` | `tasks.md` | 拆成可执行任务，将方案分解为原子级、可并行的开发任务，每个任务含明确验收条件 |
| 6. 一致性分析 | `/speckit-analyze` | 分析报告 | 确保文档一致 |
| 7. 自动实现 | `/speckit-implement` | 代码 + 文档 | 逐任务实现，AI 自动执行任务：生成代码、测试、运行命令、代码审查支持一键重试和上下文修复 |

### 流程详解

#### 步骤 0: 项目章程

使用 `/speckit-constitution` 命令，生成 `constitution.md` 文件，定义项目的基础约束和规范：

- **技术栈约束**：规定允许使用的技术栈和框架，禁止使用的技术
- **安全策略**：数据加密、身份认证、权限控制、日志审计等安全要求
- **编码规范**：命名规范、代码风格、注释规范、文件组织结构
- **架构原则**：分层架构、模块化设计、依赖倒置、单一职责等设计原则
- **开发流程**：Git 分支策略、代码审查流程、CI/CD 流程
- **质量标准**：代码覆盖率、性能指标、兼容性要求

项目章程是整个开发的基础，相当于项目的"宪法"，所有后续开发都应遵循。

#### 步骤 1: 需求规格化

使用 `/speckit-specify` 命令，生成 `spec.md` 文件，明确：

- 功能目标和用户需求
- 验收标准和成功指标
- 技术约束和依赖项
- 交付物清单

#### 步骤 2: 需求澄清

使用 `/speckit-clarify` 命令，针对需求中的不清晰点进行提问：

- 边界条件和异常处理
- 性能和安全要求
- 兼容性和可扩展性需求
- 更新 `spec.md` 文档

#### 步骤 3: 技术规划

使用 `/speckit-plan` 命令，生成 `plan.md` 文件，包含：

- 接口设计（API 契约）
- 数据模型设计
- 架构设计和技术选型
- 数据库设计

#### 步骤 4: 完整性检查

使用 `/speckit-checklist` 命令，生成 `checklist.md`，检查：

- 功能覆盖度
- 异常场景处理
- 边界条件考虑
- 安全性检查

#### 步骤 5: 任务拆解

使用 `/speckit-tasks` 命令，生成 `tasks.md`，将需求拆解为：

- 前端开发任务
- 后端开发任务
- 测试任务
- 部署任务

#### 步骤 6: 一致性分析

使用 `/speckit-analyze` 命令，生成一致性分析报告：

- 文档间一致性
- 接口与实现一致性
- 需求与测试一致性
- 识别潜在问题

#### 步骤 7: 实施执行

使用 `/speckit-implement` 命令，执行开发：

- 按任务列表逐项实现
- 生成代码和文档
- 自动化测试
- 代码审查

## SDD 的关键优势

SDD 开发模式相比传统开发具有以下核心优势：

- ? **减少认知负荷**：开发者无需记忆细节，专注逻辑定义
- ? **提升一致性**：规范统一，避免"各写各的"风格混乱
- ? **加速交付**：AI 并行生成多文件代码，效率倍增
- ? **降低缺陷率**：规范先行，边界条件提前考虑
- ? **知识沉淀**：规范文档即项目知识库，便于交接维护

### 优势详解

#### 1. 减少认知负荷
传统开发需要开发者记忆大量实现细节，SDD 将注意力转移到逻辑定义，降低记忆负担。

#### 2. 提升一致性
统一规范确保团队所有成员生成代码风格一致，避免代码风格混乱。

#### 3. 加速交付
AI 可以并行生成多个文件，大幅提升开发速度，缩短交付周期。

#### 4. 降低缺陷率
通过规范先行的方式，在编码前就考虑边界条件和异常场景，减少 Bug。

#### 5. 知识沉淀
规范文档成为项目知识库，新人快速上手，项目交接更顺畅。

## 适用场景

### 适用场景

- ? **新项目从零启动**（最佳实践）
- ? **功能模块重构**（先写新规范，再生成新实现）
- ? **需求明确但实现复杂的系统**（如数据看板、表单引擎）
- ? **团队协作开发**（统一规范 = 统一理解）

### 不适用场景

- ?? **探索性原型**：需求不明确，需要快速试错
- ?? **算法研究**：需要大量实验和调试
- ?? **高度依赖人工调试的图形/音视频处理**：需要精细调优

## 典型工具支持

### 1. Spec Kit
GitHub 官方推出的规范管理工具，提供 `/speckit.xxx` 系列命令，支持：
- 规范文档生成
- 需求分析
- 任务拆解
- 代码生成

### 2. AI 编程助手
可作为 SDD 的执行引擎：
- **Claude**：强大的代码生成和推理能力
- **Copilot**：GitHub 官方 AI 助手
- **Cursor**：专注于代码编辑的 AI 助手
- **Gemini**：Google 的多模态 AI

### 3. IDE 集成
主流编辑器均支持 SDD 工作流：
- **VS Code**：丰富的插件生态，支持多种 AI 助手
- **JetBrains 系列**：IntelliJ IDEA、WebStorm 等
- **其他编辑器**：Vim、Neovim 等

## 开发流程

### 1. 设计稿准备

```bash
# 导出设计资源
- 图标资源
- 图片资源
- 配色方案
- 字体规范
```

### 2. 页面搭建

使用 Vue3 + Element-Plus + UnoCSS 快速搭建页面：

```vue
<template>
  <micro-page title="页面标题">
    <micro-card>
      <!-- 页面内容 -->
    </micro-card>
  </micro-page>
</template>

<script setup lang="ts">
// 页面逻辑
</script>
```

### 3. 组件使用

```typescript
import { MicroButton, MicroTable, MicroForm } from '@micro/components'

// 直接使用组件
```

## 代码生成

### 使用代码生成器

1. 配置数据表信息
2. 选择生成模板
3. 一键生成前后端代码

生成内容包括：
- Vue 页面组件
- API 接口
- 类型定义
- 路由配置

## 最佳实践

### 1. 命名规范

```typescript
// 组件命名
PascalCase: UserList.vue

// 文件命名
kebab-case: user-list.ts

// 变量命名
camelCase: userName
```

### 2. 组件封装

```vue
<script setup lang="ts">
// 使用 defineProps 定义接口
interface Props {
  data: User[]
  loading?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  loading: false
})
</script>
```

### 3. 类型安全

```typescript
// 使用 TypeScript 类型定义
interface User {
  id: number
  name: string
  email: string
}

// API 返回类型
type ApiResponse<T> = {
  code: number
  data: T
  message: string
}
```

## 规范文档

详见：
- [代码规范](./use-icons.md)
- [目录结构](./directory-structure.md)
- [权限控制](./permission-control.md)

## 相关链接

- [组件库文档](https://github.com/micro-components/micro-design-admin)

## 七、IDE 开发教程

### Cursor 教程

#### 环境准备

1. **安装 Cursor**
   - 访问 [cursor.sh](https://cursor.sh) 下载安装
   - 支持 Windows、macOS、Linux

2. **配置 API Key**
   - 打开 Cursor 设置（Ctrl/Cmd + ,）
   - 找到 AI Provider 设置
   - 选择 Claude 或 OpenAI
   - 输入对应的 API Key

#### SDD 工作流

##### 步骤 1：创建项目章程

```
在 Cursor 中打开项目，使用 Composer (Ctrl/Cmd + I)

提示词：
"请为我创建一个项目章程文档 constitution.md，包含以下内容：
- 技术栈约束
- 安全策略
- 编码规范
- 架构原则
- 开发流程
- 质量标准"
```

##### 步骤 2：需求规格化

```
使用 Chat 面板 (Ctrl/Cmd + L)

提示词：
/speckit-specify

我要开发一个用户管理模块，包含用户列表、新增、编辑、删除、批量操作功能，请生成 spec.md 规范文档。
```

##### 步骤 3：代码生成

```
使用 Composer 选中 spec.md

提示词：
"根据这个需求规范文档，生成完整的前端代码，包括：
- 用户列表页面组件
- 新增/编辑表单组件
- API 接口文件
- 类型定义文件
- 路由配置"
```

#### Cursor 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl/Cmd + K` | 单行/选中区域 AI 编辑 |
| `Ctrl/Cmd + L` | 打开 Chat 面板 |
| `Ctrl/Cmd + I` | 打开 Composer |
| `Ctrl/Cmd + Shift + K` | 智能搜索项目 |

### Claude Code 教程

#### 环境准备

1. **安装 Claude Code**
   - 确保已安装 Node.js (v18+)
   - 使用 npm 全局安装：
     ```bash
     npm install -g @anthropic-ai/claude-code
     ```

2. **初始化配置**
   ```bash
   claude-code init
   ```
   - 输入 Anthropic API Key
   - 选择默认模型

#### SDD 工作流

##### 步骤 1：创建项目目录

```bash
# 创建项目结构
mkdir -p project/docs
cd project
```

##### 步骤 2：生成项目章程

```bash
claude-code "请为我创建项目章程文档 docs/constitution.md，包含技术栈、安全策略、编码规范、架构原则"
```

##### 步骤 3：需求规格化

```bash
claude-code "/speckit-specify 我要开发用户管理模块" > docs/spec.md
```

##### 步骤 4：技术规划

```bash
claude-code "/speckit-plan 基于 docs/spec.md" > docs/plan.md
```

##### 步骤 5：代码生成

```bash
# 生成前端代码
claude-code "/speckit-implement 根据 docs/plan.md 生成前端代码"

# 生成后端代码
claude-code "/speckit-implement 根据 docs/plan.md 生成后端代码"
```

#### Claude Code 常用命令

```bash
# 查看帮助
claude-code --help

# 指定模型
claude-code --model claude-3-5-sonnet "提示词"

# 从文件读取输入
claude-code "$(cat docs/spec.md)"

# 输出到文件
claude-code "生成代码" > output.ts
```

### VS Code + Copilot 教程

#### 环境准备

1. **安装 GitHub Copilot**
   - 打开 VS Code 扩展市场
   - 搜索 "GitHub Copilot"
   - 点击安装

2. **登录授权**
   - 安装后点击 Sign in
   - 使用 GitHub 账号登录
   - 授权 Copilot 访问权限

#### SDD 工作流

##### 步骤 1：创建文档模板

在 VS Code 中创建 `docs/constitution.md`，使用 Copilot Chat 生成内容：

```
@workspace 请帮我生成项目章程文档，包含技术栈、安全策略、编码规范、架构原则
```

##### 步骤 2：生成需求规范

创建 `docs/spec.md`，使用 Copilot 编辑：

```
# 在文档中输入注释
// 我要开发用户管理模块，包含CRUD操作

# 按 Tab 键接受 Copilot 建议
```

##### 步骤 3：代码生成

```typescript
// 在新文件中写注释，Copilot 会自动补全
// 用户列表页面组件，根据 docs/spec.md 生成

import { ref, onMounted } from 'vue'
// ... Copilot 自动补全剩余代码
```

#### Copilot 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Tab` | 接受单行建议 |
| `Ctrl/Cmd + →` | 接受整段建议 |
| `Ctrl/Cmd + ]` | 显示下一个建议 |
| `Ctrl/Cmd + [` | 显示上一个建议 |
| `Ctrl/Cmd + Shift + \` | 打开 Copilot Chat |

## 八、文档模板

### 1. 项目章程模板 (constitution.md)

```markdown
# 项目章程 (Constitution)

> 本文档是项目的基础规范，相当于项目的"宪法"，所有开发活动应遵循本章程。

## 1. 技术栈约束

### 前端技术栈
- **框架**: Vue 3.4+
- **语言**: TypeScript 5.0+
- **构建工具**: Vite 5.0+
- **UI 库**: Element Plus
- **样式**: UnoCSS
- **状态管理**: Pinia
- **路由**: Vue Router 4.x
- **HTTP**: Axios

### 后端技术栈
- **语言**: Java 17+
- **框架**: Spring Boot 3.x
- **数据库**: MySQL 8.0+
- **ORM**: MyBatis Plus
- **安全**: Spring Security + JWT
- **文档**: SpringDoc OpenAPI

### 禁用技术
- 禁止使用 jQuery
- 禁止使用 class 组件风格
- 禁止使用 any 类型

## 2. 安全策略

### 数据安全
- 所有敏感数据必须加密存储
- API 接口必须鉴权
- 密码使用 BCrypt 加密
- Token 有效期不超过 2 小时

### 代码安全
- 禁止 SQL 拼接，使用参数化查询
- 禁止在代码中硬编码密钥
- 输入必须校验和过滤
- XSS 和 CSRF 防护

## 3. 编码规范

### 命名规范
- 组件：PascalCase (UserList.vue)
- 文件：kebab-case (user-service.ts)
- 变量：camelCase (userName)
- 常量：UPPER_SNAKE_CASE (MAX_COUNT)
- 类型：PascalCase (UserType)

### 代码风格
- 使用 2 空格缩进
- 使用单引号
- 语句末尾加分号
- 函数不超过 50 行
- 复杂逻辑必须加注释

### Git 规范
- 分支命名：feature/user-management
- 提交信息：feat: add user list
- 必须通过 ESLint 检查
- 必须通过单元测试

## 4. 架构原则

### 分层架构
- Controller 层：接收请求，返回响应
- Service 层：业务逻辑
- Mapper 层：数据访问

### 设计原则
- 单一职责原则
- 开闭原则
- 依赖倒置原则
- 接口隔离原则

## 5. 开发流程

### 分支策略
- main：生产环境
- develop：开发环境
- feature/*：功能分支
- hotfix/*：修复分支

### CI/CD
- 代码提交自动构建
- 自动运行测试
- 自动部署到测试环境
- 主分支合并自动部署到生产环境

## 6. 质量标准

### 代码质量
- 单元测试覆盖率 >= 80%
- ESLint 0 错误
- TypeScript 0 错误

### 性能标准
- 首屏加载 < 2s
- API 响应 < 500ms
- 页面 FPS >= 60

### 兼容性
- Chrome 最新 2 版本
- Edge 最新 2 版本
- Firefox 最新 2 版本
```

### 2. 需求规范模板 (spec.md)

```markdown
# 需求规范 (Specification)

## 功能概述

**模块名称**: 用户管理模块

**功能描述**: 实现用户的增删改查、批量操作、状态管理等功能

**目标用户**: 系统管理员

## 功能需求

### 1. 用户列表

#### 1.1 查询功能
- 支持按用户名搜索
- 支持按邮箱搜索
- 支持按状态筛选（正常/禁用）
- 支持按创建时间范围查询

#### 1.2 列表展示
- 展示用户基本信息：ID、用户名、邮箱、状态、创建时间
- 支持分页，默认每页 20 条
- 支持按创建时间倒序排列
- 空状态提示

### 2. 新增用户

#### 2.1 表单字段
- 用户名：必填，2-20 字符，唯一性校验
- 邮箱：必填，邮箱格式校验，唯一性校验
- 密码：必填，6-20 字符，包含字母和数字
- 手机号：选填，11 位手机号
- 状态：默认正常

#### 2.2 校验规则
- 用户名不能包含特殊字符
- 邮箱必须是有效格式
- 密码强度校验

### 3. 编辑用户

#### 3.1 可编辑字段
- 用户名：不可编辑
- 邮箱：可编辑，需校验唯一性
- 手机号：可编辑
- 状态：可切换

#### 3.2 校验规则
- 与新增相同校验规则

### 4. 删除用户

#### 4.1 删除规则
- 软删除，不真正删除数据
- 删除前弹出确认对话框
- 删除成功提示
- 记录删除日志

### 5. 批量操作

#### 5.1 批量删除
- 支持多选用户
- 批量删除需二次确认
- 删除数量提示

#### 5.2 批量禁用/启用
- 支持批量切换用户状态
- 操作成功提示

## 非功能需求

### 性能要求
- 列表加载时间 < 1s
- 搜索响应时间 < 500ms
- 支持 10 万级用户数据

### 安全要求
- 只有管理员可访问
- 敏感操作需二次确认
- 记录操作日志
- 防止 XSS 攻击

### 兼容性要求
- Chrome 90+
- Edge 90+
- Firefox 88+

## 验收标准

### 功能验收
- [ ] 用户列表正常显示
- [ ] 搜索功能正常
- [ ] 新增用户成功
- [ ] 编辑用户成功
- [ ] 删除用户成功
- [ ] 批量操作成功

### 性能验收
- [ ] 列表加载 < 1s
- [ ] 搜索响应 < 500ms

### 安全验收
- [ ] 权限控制正常
- [ ] XSS 防护有效
- [ ] 操作日志完整

## 依赖项

### 前置条件
- 用户已登录
- 用户有管理员权限

### 外部依赖
- 用户管理 API 接口
- 权限管理服务

## 交付物

- 前端页面组件
- API 接口封装
- 单元测试用例
- 用户操作文档
```

### 3. 技术规划模板 (plan.md)

```markdown
# 技术规划 (Technical Plan)

## API 设计

### 1. 用户列表接口

#### GET /api/users
获取用户列表

**请求参数:**
```json
{
  "username": "string",    // 用户名（可选）
  "email": "string",       // 邮箱（可选）
  "status": "number",      // 状态：0-禁用，1-正常（可选）
  "startTime": "string",   // 开始时间（可选）
  "endTime": "string",     // 结束时间（可选）
  "page": "number",       // 页码，默认1
  "size": "number"        // 每页数量，默认20
}
```

**响应数据:**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "username": "admin",
        "email": "admin@example.com",
        "phone": "13800138000",
        "status": 1,
        "createTime": "2024-01-01 00:00:00"
      }
    ],
    "total": 100,
    "page": 1,
    "size": 20
  }
}
```

### 2. 新增用户接口

#### POST /api/users
创建新用户

**请求体:**
```json
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "123456",
  "phone": "13800138000"
}
```

**响应数据:**
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1
  }
}
```

### 3. 编辑用户接口

#### PUT /api/users/{id}
更新用户信息

**请求体:**
```json
{
  "email": "newemail@example.com",
  "phone": "13800138001",
  "status": 1
}
```

### 4. 删除用户接口

#### DELETE /api/users/{id}
删除用户（软删除）

**响应数据:**
```json
{
  "code": 200,
  "message": "删除成功"
}
```

### 5. 批量删除接口

#### DELETE /api/users/batch
批量删除用户

**请求体:**
```json
{
  "ids": [1, 2, 3]
}
```

## 数据模型

### User 表

```sql
CREATE TABLE `user` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `email` varchar(100) NOT NULL COMMENT '邮箱',
  `password` varchar(255) NOT NULL COMMENT '密码',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `status` tinyint NOT NULL DEFAULT 1 COMMENT '状态：0-禁用，1-正常',
  `deleted` tinyint NOT NULL DEFAULT 0 COMMENT '删除标记：0-未删除，1-已删除',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_email` (`email`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### TypeScript 类型定义

```typescript
// 用户信息接口
export interface User {
  id: number
  username: string
  email: string
  phone?: string
  status: UserStatus
  createTime: string
}

// 用户状态枚举
export enum UserStatus {
  DISABLED = 0,
  NORMAL = 1
}

// 用户查询参数
export interface UserQuery {
  username?: string
  email?: string
  status?: UserStatus
  startTime?: string
  endTime?: string
  page: number
  size: number
}

// 新增用户接口
export interface CreateUserDto {
  username: string
  email: string
  password: string
  phone?: string
}

// 更新用户接口
export interface UpdateUserDto {
  email?: string
  phone?: string
  status?: UserStatus
}
```

## 组件设计

### 页面组件结构

```
src/views/user/
├── index.vue              # 用户列表页面
├── components/
│   ├── UserForm.vue       # 用户表单组件
│   ├── UserFilter.vue     # 筛选组件
│   └── UserActions.vue    # 操作按钮组件
└── api/
    └── user.ts          # 用户 API 接口
```

### 组件接口

#### UserForm.vue

```typescript
interface Props {
  mode: 'add' | 'edit'
  userId?: number
}

interface Emits {
  (e: 'success'): void
  (e: 'cancel'): void
}
```

## 状态管理

```typescript
// stores/user.ts
export const useUserStore = defineStore('user', () => {
  const users = ref<User[]>([])
  const loading = ref(false)

  // 获取用户列表
  const fetchUsers = async (query: UserQuery) => {
    loading.value = true
    try {
      const res = await userApi.getList(query)
      users.value = res.data.records
      return res
    } finally {
      loading.value = false
    }
  }

  // 删除用户
  const deleteUser = async (id: number) => {
    await userApi.delete(id)
    ElMessage.success('删除成功')
  }

  return {
    users,
    loading,
    fetchUsers,
    deleteUser
  }
})
```

## 路由配置

```typescript
// router/modules/user.ts
export default {
  path: '/user',
  name: 'User',
  component: Layout,
  redirect: '/user/list',
  meta: {
    title: '用户管理',
    icon: 'user',
    roles: ['admin']
  },
  children: [
    {
      path: 'list',
      name: 'UserList',
      component: () => import('@/views/user/index.vue'),
      meta: {
        title: '用户列表',
        cache: true
      }
    }
  ]
}
```
```

### 4. 完整性检查清单模板 (checklist.md)

```markdown
# 完整性检查清单 (Checklist)

## 功能完整性

### 基础功能
- [ ] 列表查询功能正常
- [ ] 搜索功能正常
- [ ] 分页功能正常
- [ ] 排序功能正常

### CRUD 操作
- [ ] 新增功能正常
- [ ] 编辑功能正常
- [ ] 删除功能正常
- [ ] 批量操作正常

### 校验功能
- [ ] 必填项校验
- [ ] 格式校验（邮箱、手机号等）
- [ ] 唯一性校验
- [ ] 长度校验

### 状态处理
- [ ] 加载状态显示
- [ ] 空状态提示
- [ ] 错误状态处理
- [ ] 成功提示

## 边界条件

### 输入边界
- [ ] 空值处理
- [ ] 超长输入处理
- [ ] 特殊字符处理
- [ ] SQL 注入防护
- [ ] XSS 防护

### 数据边界
- [ ] 无数据情况
- [ ] 大量数据（1000+）处理
- [ ] 并发请求处理
- [ ] 重复提交防护

### 性能边界
- [ ] 列表加载性能
- [ ] 搜索响应性能
- [ ] 内存占用检查
- [ ] 懒加载实现

## 异常场景

### 网络异常
- [ ] 网络断开处理
- [ ] 请求超时处理
- [ ] 服务器错误处理
- [ ] 重试机制

### 业务异常
- [ ] 用户不存在
- [ ] 数据已存在
- [ ] 权限不足
- [ ] 状态冲突

### 系统异常
- [ ] 内存溢出处理
- [ ] 磁盘空间不足
- [ ] 数据库连接失败
- [ ] 依赖服务不可用

## 安全检查

### 认证授权
- [ ] 登录验证
- [ ] 权限校验
- [ ] Token 有效期检查
- [ ] 敏感操作二次确认

### 数据安全
- [ ] 密码加密存储
- [ ] 敏感信息脱敏
- [ ] SQL 注入防护
- [ ] XSS 防护
- [ ] CSRF 防护

### 操作日志
- [ ] 登录日志
- [ ] 操作日志
- [ ] 异常日志
- [ ] 审计日志

## 兼容性检查

### 浏览器兼容
- [ ] Chrome 最新版
- [ ] Firefox 最新版
- [ ] Edge 最新版
- [ ] Safari 最新版

### 分辨率适配
- [ ] 1920x1080
- [ ] 1366x768
- [ ] 移动端适配
- [ ] 响应式布局

### 设备兼容
- [ ] PC 端
- [ ] 平板端
- [ ] 移动端

## 用户体验

### 交互体验
- [ ] 加载动画
- [ ] 过渡动画
- [ ] 提示信息友好
- [ ] 错误提示清晰

### 可访问性
- [ ] 键盘导航
- [ ] 屏幕阅读器支持
- [ ] 色彩对比度
- [ ] 字体大小可调

### 性能体验
- [ ] 首屏加载 < 2s
- [ ] 页面响应 < 500ms
- [ ] 动画流畅 60fps
- [ ] 懒加载实现

## 测试覆盖

### 单元测试
- [ ] 工具函数测试
- [ ] 组件测试
- [ ] Service 测试
- [ ] 覆盖率 >= 80%

### 集成测试
- [ ] API 接口测试
- [ ] 数据库操作测试
- [ ] 第三方服务测试

### E2E 测试
- [ ] 主流程测试
- [ ] 异常流程测试
- [ ] 跨浏览器测试

## 文档完整性

### 代码文档
- [ ] 组件注释
- [ ] 函数注释
- [ ] 复杂逻辑注释
- [ ] README 文档

### 用户文档
- [ ] 功能说明
- [ ] 使用教程
- [ ] 常见问题
- [ ] 更新日志

### API 文档
- [ ] 接口说明
- [ ] 请求参数
- [ ] 响应示例
- [ ] 错误码说明
```

### 5. 任务拆解模板 (tasks.md)

```markdown
# 任务拆解 (Tasks)

## 任务总览

**目标**: 完成用户管理模块开发

**预计工期**: 5 个工作日

**负责人**: 开发团队

## 前端开发任务

### 1. 基础框架搭建 (0.5 天)

#### 1.1 创建目录结构
- [ ] 创建 views/user 目录
- [ ] 创建 components/user 目录
- [ ] 创建 api/user.ts 文件

**负责人**: 张三
**验收标准**: 目录结构符合规范

#### 1.2 配置路由
- [ ] 配置用户管理路由
- [ ] 配置权限校验
- [ ] 配置页面缓存

**负责人**: 张三
**验收标准**: 路由可访问，权限生效

### 2. API 接口封装 (0.5 天)

#### 2.1 定义接口类型
- [ ] 定义 User 类型
- [ ] 定义 UserQuery 类型
- [ ] 定义 CreateUserDto 类型
- [ ] 定义 UpdateUserDto 类型

**负责人**: 张三
**验收标准**: 类型定义完整，无错误

#### 2.2 封装 API 方法
- [ ] getList 方法
- [ ] getById 方法
- [ ] create 方法
- [ ] update 方法
- [ ] delete 方法
- [ ] batchDelete 方法

**负责人**: 张三
**验收标准**: 所有 API 方法可调用

### 3. 用户列表页面 (1 天)

#### 3.1 实现筛选组件
- [ ] 用户名输入框
- [ ] 邮箱输入框
- [ ] 状态下拉选择
- [ ] 时间范围选择器
- [ ] 搜索按钮
- [ ] 重置按钮

**负责人**: 李四
**验收标准**: 筛选功能正常

#### 3.2 实现表格组件
- [ ] 表格列配置
- [ ] 分页组件
- [ ] 加载状态
- [ ] 空状态提示
- [ ] 操作列（编辑、删除）

**负责人**: 李四
**验收标准**: 表格展示正常，分页功能可用

#### 3.3 实现批量操作
- [ ] 复选框
- [ ] 批量删除按钮
- [ ] 批量禁用/启用按钮
- [ ] 确认对话框

**负责人**: 李四
**验收标准**: 批量操作正常

### 4. 用户表单组件 (1 天)

#### 4.1 新增表单
- [ ] 用户名输入
- [ ] 邮箱输入
- [ ] 密码输入
- [ ] 手机号输入
- [ ] 表单校验
- [ ] 提交按钮

**负责人**: 王五
**验收标准**: 新增功能正常，校验生效

#### 4.2 编辑表单
- [ ] 数据回显
- [ ] 用户名禁用
- [ ] 表单校验
- [ ] 提交按钮

**负责人**: 王五
**验收标准**: 编辑功能正常，数据正确回显

## 后端开发任务

### 1. 数据库设计 (0.5 天)

#### 1.1 创建表结构
- [ ] 创建 user 表
- [ ] 创建索引
- [ ] 添加外键约束（如需要）

**负责人**: 赵六
**验收标准**: 表结构符合设计文档

#### 1.2 初始化数据
- [ ] 创建管理员账号
- [ ] 创建测试数据

**负责人**: 赵六
**验收标准**: 可正常登录

### 2. Entity 层 (0.5 天)

#### 2.1 创建实体类
- [ ] User 实体类
- [ ] 字段映射
- [ ] 注解配置

**负责人**: 赵六
**验收标准**: 实体类符合规范

### 3. Mapper 层 (1 天)

#### 3.1 创建 Mapper 接口
- [ ] UserMapper 接口
- [ ] 继承 BaseMapper

**负责人**: 赵六
**验收标准**: Mapper 接口创建成功

#### 3.2 创建 XML 映射文件
- [ ] 自定义查询方法
- [ ] 分页查询
- [ ] 条件查询

**负责人**: 赵六
**验收标准**: 所有查询方法可用

### 4. Service 层 (1 天)

#### 4.1 UserService 接口
- [ ] 接口定义
- [ ] 方法声明

**负责人**: 赵六
**验收标准**: 接口定义完整

#### 4.2 UserServiceImpl 实现
- [ ] 分页查询实现
- [ ] 新增实现
- [ ] 更新实现
- [ ] 删除实现
- [ ] 批量删除实现
- [ ] 业务逻辑校验

**负责人**: 赵六
**验收标准**: 所有方法实现正确

### 5. Controller 层 (0.5 天)

#### 5.1 UserController
- [ ] 列表查询接口
- [ ] 详情查询接口
- [ ] 新增接口
- [ ] 更新接口
- [ ] 删除接口
- [ ] 批量删除接口
- [ ] 参数校验

**负责人**: 赵六
**验收标准**: 所有接口可访问

## 测试任务

### 1. 单元测试 (0.5 天)

- [ ] Service 层测试
- [ ] 覆盖率 >= 80%

**负责人**: 测试团队

### 2. 接口测试 (0.5 天)

- [ ] 所有接口测试
- [ ] 异常场景测试
- [ ] 性能测试

**负责人**: 测试团队

### 3. 集成测试 (0.5 天)

- [ ] 前后端联调
- [ ] 完整流程测试
- [ ] Bug 修复

**负责人**: 开发团队

## 文档任务 (0.5 天)

- [ ] API 文档
- [ ] 用户操作手册
- [ ] 部署文档

**负责人**: 技术文档组

## 验收标准

### 功能验收
- [ ] 所有功能正常运行
- [ ] 所有测试用例通过
- [ ] 无 P0 级 Bug
- [ ] P1 级 Bug < 5 个

### 性能验收
- [ ] 接口响应 < 500ms
- [ ] 页面加载 < 2s
- [ ] 并发支持 100+ 用户

### 文档验收
- [ ] API 文档完整
- [ ] 用户手册完整
- [ ] 代码注释完整
```

### 6. 项目目录结构模板

```
project-root/
├── .speckit/                    # SDD 规范目录
│   ├── constitution.md          # 项目章程
│   ├── spec.md                 # 需求规范
│   ├── plan.md                 # 技术规划
│   ├── checklist.md            # 完整性检查清单
│   └── tasks.md               # 任务拆解
│
├── docs/                       # 文档目录
│   ├── api/                   # API 文档
│   ├── design/                # 设计文档
│   └── user/                 # 用户手册
│
├── src/                        # 源代码
│   ├── api/                   # API 接口
│   ├── assets/                # 静态资源
│   ├── components/            # 公共组件
│   ├── composables/           # 组合式函数
│   ├── layouts/               # 布局组件
│   ├── router/                # 路由配置
│   ├── stores/               # 状态管理
│   ├── styles/               # 样式文件
│   ├── utils/                # 工具函数
│   └── views/               # 页面组件
│
├── tests/                     # 测试文件
│   ├── unit/                # 单元测试
│   └── e2e/                # E2E 测试
│
├── .gitignore                # Git 忽略配置
├── package.json             # 项目配置
├── tsconfig.json           # TypeScript 配置
├── vite.config.ts          # Vite 配置
└── README.md              # 项目说明
```
